# EVAL: 2026-04-19 Session Improvements

세션 중 발견한 버그/개선점을 회귀 방지 + capability + backlog로 분류.
모든 grader는 가능한 deterministic (code-based).

## Phase A: Regression Evals (이미 fix — 회귀 방지)

### R1. csr-supabase에 Railway MCP 미설치
**Why fixed (v27.4.0)**: Supabase는 자체 backend (Edge Functions). Railway는 redundant.

**Grader (code)**:
```bash
TMP=$(mktemp -d) && cd "$TMP" && git init -q && echo "# T" > README.md && git add . && git -c user.email=t@t -c user.name=t commit -m init -q
bash $REPO/scripts/setup-harness.sh --track csr-supabase --project-dir . < /dev/null > /tmp/r1.log 2>&1

# 기대: railway-mcp-server 미존재
jq '.mcpServers | has("railway-mcp-server")' .mcp.json
# Expected: false
```

**Pass criterion**: `false`. fail이면 track-mcp-map.tsv 회귀.

### R2-4. --add-track 실행 시 ECC/ToB/GSD 프롬프트 안 뜸
**Why fixed (v27.5.0)**: ADD_MODE skip 가드 추가.

**Grader (code)**:
```bash
TMP=$(mktemp -d) && cd "$TMP" && git init -q && README.md ... commit -q
bash $REPO/scripts/setup-harness.sh --track tooling --project-dir . < /dev/null > /tmp/init.log 2>&1
# 이제 add-track — 절대 read 멈춤 없어야 (timeout 30초로)
timeout 60 bash $REPO/scripts/setup-harness.sh --add-track csr-supabase --project-dir . < /dev/null > /tmp/r2.log 2>&1
EXIT=$?

# Pass: timeout 안 걸리고 exit 0 + ECC/ToB/GSD 프롬프트 문구 출력 없음
[ "$EXIT" -eq 0 ] && \
  ! grep -q "ECC plugin을 프로젝트 스코프\|Trail of Bits security\|Install GSD" /tmp/r2.log && \
  echo PASS || echo FAIL
```

**Pass criterion**: PASS. 프롬프트 문구가 한 줄도 없어야.

### R5. curl|bash + TTY 환경에서 ECC 프롬프트 진입
**Why (v27.2.0)**: `[ -e /dev/tty ]` 검사 추가.

**Grader**: 수동 (실제 curl|bash 실행 + `n` 응답 확인). 자동화 불가 — model-based 또는 human grader.

### R6. 인터랙티브 read 모두 `< /dev/tty` 사용
**Why (v27.5.0)**: GSD 프롬프트 read에 < /dev/tty 누락 발견 → 추가.

**Grader (code)**:
```bash
# scripts/setup-harness.sh의 사용자 input read 4개가 모두 < /dev/tty 사용
COUNT=$(grep -cE 'read.*-rp.*ANSWER\|TRACK_NUM.*< /dev/tty' scripts/setup-harness.sh)
# Track 선택 read는 [ ! -t 0 ] 가드라 < /dev/tty 불필요. 4 - 1 = 3 expected (GSD/ToB/ECC/PRUNE → 4개 중 PRUNE은 ECC 안 nested).
# 더 정확:
grep -nE 'read -rp.*(GSD_ANSWER|TOB_ANSWER|ECC_ANSWER|PRUNE_ANSWER)' scripts/setup-harness.sh | grep -c "< /dev/tty"
# Expected: 4
```

**Pass criterion**: 4개 모두 `< /dev/tty`.

---

## Phase B: Capability Evals (신규 동작 검증)

### C1. --add-track --with-ecc 시 ECC 진행
**Behavior**: ADD_MODE에서도 명시 플래그면 ECC 설치.

**Grader**:
```bash
# 명시 플래그로 ECC 설치 진행하는지 (실제 install은 네트워크라 dry-check)
timeout 90 bash $REPO/scripts/setup-harness.sh --add-track csr-supabase --with-ecc --project-dir . < /dev/null > /tmp/c1.log 2>&1
grep -q "with-ecc 플래그 감지\|ECC plugin을 프로젝트 스코프" /tmp/c1.log && echo PASS || echo FAIL
```

### C2. csr-supabase install시 Vercel + Netlify CLI 시도
**Behavior**: csr-supabase에 Vercel/Netlify CLI 설치 라인 도달.

**Grader (code)**:
```bash
# setup-harness.sh dry-grep
grep -q "Vercel CLI" scripts/setup-harness.sh && \
  grep -q "Netlify CLI" scripts/setup-harness.sh && \
  grep -q "any_track 'csr-supabase|full'" scripts/setup-harness.sh && \
  echo PASS || echo FAIL
```

### C3. /uzys:plan에 Plan Depth 가이드 (v26.16.1)
**Behavior**: trivial/standard/complex 분기 가이드.

**Grader (code)**:
```bash
grep -q "Plan Depth" templates/commands/uzys/plan.md && \
  grep -q "Trivial" templates/commands/uzys/plan.md && \
  echo PASS || echo FAIL
```

---

## Phase C: Backlog (미해결 개선점)

### B1. CL-v2 instinct promotion 첫 사례 (P0)
**From**: deep-research 2026-04-18 §3 ("Instinct promotion stub, precedent 0")

**Why**: 학습 루프가 stub. 실제 promote된 instinct 0건. credibility 위해 1-2건 필요.

**Action**: 자연 누적 후 `/ecc:promote` → `docs/decisions/ADR-N-*.md` 기록.

**Grader (code)**:
```bash
# docs/decisions/ 안에 instinct-promotion ADR 1건 이상
find docs/decisions/ -name "ADR-*-instinct*" 2>/dev/null | wc -l
# Expected: ≥ 1
```

**Status**: NOT STARTED. 자연 누적 기다림.

### B2. prune-ecc.sh line 228 read에 < /dev/tty 추가
**From**: 이번 세션 audit (잠재 hang 가능)

**Why**: `read -rp "  진행? [y/N]: " ANSWER` — `--force` 미명시 + curl|bash 환경에서 hang 가능.

**Action (1줄 fix)**:
```bash
read -rp "  진행? [y/N]: " ANSWER < /dev/tty 2>/dev/null || ANSWER=""
```

**Grader**:
```bash
grep -E 'read -rp.*ANSWER' scripts/prune-ecc.sh | grep -q '< /dev/tty' && echo PASS || echo FAIL
```

**Status**: PENDING. 한 줄 fix.

### B3. CHANGELOG v27.x release entries 누락
**From**: 이번 세션 audit. v27.2.0~v27.5.0이 [v27.0.0]만 있고 그 사이 release entries 없음.

**Action**: CHANGELOG.md에 v27.5.0/v27.4.0/v27.3.0/v27.2.0/v27.1.0 entries 추가.

**Grader**:
```bash
COUNT=$(grep -cE "^## \\[v27\\.[2-5]\\." CHANGELOG.md)
# Expected: ≥ 4
```

**Status**: PENDING.

### B4. README badge "111 PASS" 자동 동기화
**From**: 이번 세션 audit. README badge가 hardcoded — test 늘어나면 stale.

**Action**: 옵션 (a) badge 제거 (b) GitHub Actions에서 동적 생성 (c) 매 release마다 sed 업데이트

**Grader**:
```bash
# 현재 test count vs badge 일치
ACTUAL=$(bash scripts/test-harness.sh 2>&1 | grep -oE "Pass: [0-9]+" | head -1 | grep -oE "[0-9]+")
BADGE=$(grep -oE "tests-[0-9]+%20PASS" README.md | grep -oE "[0-9]+" | head -1)
[ "$ACTUAL" = "$BADGE" ] && echo PASS || echo "FAIL (actual=$ACTUAL badge=$BADGE)"
```

**Status**: PENDING (가장 단순한 해결: 매번 manual sed 또는 badge 제거).

### B5. AC ↔ code traceability matcher
**From**: deep-research 2026-04-18 §4 ("LLM judge 없으면 false positive 위험" → 보류)

**Action**: 보류 — LLM judge 도입 또는 outcome-based 매칭 설계 필요.

**Status**: DEFERRED.

---

## Pass Metrics 목표

| Eval Type | Target | 현재 |
|-----------|:-:|:-:|
| Regression (R1-R6) | pass^3 = 100% | TBD (수동 검증 필요) |
| Capability (C1-C3) | pass@1 ≥ 100% | TBD |
| Backlog (B1-B5) | N/A (작업 항목) | 0/5 완료 |

## Run All

```bash
# 전체 자동 grader 실행 (수작업 grader 제외)
bash .claude/evals/run.sh session-2026-04-19  # (run.sh 별도 작성 필요 시)
```

## 즉시 가능 fix (이 eval 정의 후 별도 commit 권장)

- **B2** (prune-ecc.sh `< /dev/tty`): 한 줄 fix
- **B3** (CHANGELOG entries): 5분 작업
- **B4** (README badge): 가장 쉬운 옵션 = "111 PASS" 수치 → "passing" 같은 정적 문구
