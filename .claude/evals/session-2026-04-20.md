# Eval: session-2026-04-20 (v27.8.0 → v27.13.0)

## Context

7시간 세션에서 v27.6.0 → v27.13.0 진행. 8개 commit + 8개 tag + 7개 GitHub Release. 원래 4개 요청 + 보너스 2건(E2E 강화, 보안 sanitize) + 1건 자기 적용(NORTH_STAR dogfood).

본 eval은 **v27.12.0에서 확장한 eval-harness 포맷(.md + .log 쌍, Capability/Regression/Test 3섹션 의무)을 실제로 처음 적용하는 자기 참조 사례**.

---

## Capability

세션 중 새로 생긴 능력. 각 AC는 측정 가능한 pass/fail 기준.

### C1. curl|bash 설치 UX 버그 근본 해결 (v27.8.0)
- **AC-1.1**: 모든 설치 호출에 `</dev/null >/dev/null 2>&1` 일관 적용 (≥27건)
- **AC-1.2**: install.sh + setup-harness.sh에 fd 3 TTY 재부착 헤더 존재
- **AC-1.3**: stdin pipe 상태에서 `--help` 즉시 exit 0
- **AC-1.4**: background/CI에서 controlling tty 없어도 정상 동작 (set -e + Device not configured 회피)
- **Grader**: code-based — T15.1~T15.6 (6 assertion)

### C2. install.sh entry-point E2E 검증 (v27.9.0)
- **AC-2.1**: `UZYS_HARNESS_REPO=file://...`로 curl|bash 경로를 로컬 git repo로 재현 가능
- **AC-2.2**: install.sh가 clone → setup-harness → asset 생성을 end-to-end 수행, exit 0
- **AC-2.3**: 임시 클론이 프로젝트 디렉토리로 유출 안 됨 (trap cleanup)
- **AC-2.4**: T5 7 → 9 track 전수 병렬 설치 검증
- **Grader**: code-based — T16.1~T16.6 + T5 확장 (8 assertion)

### C3. north-star skill + 4-gate decision heuristic (v27.10.0)
- **AC-3.1**: `templates/skills/north-star/SKILL.md` 존재 + name/description frontmatter
- **AC-3.2**: 4-gate 키워드 모두 등장 (Trend/Persona/Capability/Lean)
- **AC-3.3**: `NORTH_STAR.template.md` 7+섹션 (NSM/Will/Won't/Trade-offs/Phase/Decision/Changelog)
- **AC-3.4**: `/uzys:spec` D블록 + `/uzys:plan` Process step 4에 결합
- **AC-3.5**: setup-harness.sh가 skill을 사용자 프로젝트로 복사 (safe_copy_dir)
- **Grader**: code-based — T17.1~T17.8 (8 assertion)

### C4. ui-visual-review skill + Review Gate 통합 (v27.11.0)
- **AC-4.1**: SKILL.md에 capture / baseline / diff / regression 4 키워드 모두 등장
- **AC-4.2**: UI Track(csr-*/ssr-*/full) 한정 명시
- **AC-4.3**: `/uzys:test`에 호출 섹션 + `/uzys:review` step 5에 REGRESSION 차단 로직
- **AC-4.4**: 출력 경로 규약 (`docs/screenshots/<phase>/*.png`, `docs/visual-review-<phase>.md`)
- **AC-4.5**: setup-harness.sh가 UI Track 한정으로 skill 복사
- **Grader**: code-based — T18.1~T18.7 (7 assertion)

### C5. GoalTrack + Vantage 외부 이식 (v27.12.0)
- **AC-5.1**: `templates/docs/PLAN.template.md`에 Milestone × Dependency Graph + Critical Path 명시
- **AC-5.2**: `change-management.md`에 ADR Status 흐름 (Proposed/Accepted/Superseded/Deprecated) + 채택 프로세스 + 대상/비대상
- **AC-5.3**: `spec-scaling/SKILL.md`에 PRD 계층화(`docs/PRD/`) + "SPEC vs PRD 분리 가이드"
- **AC-5.4**: `eval-harness/SKILL.md`에 .md+.log 쌍 의무화 + 3섹션(Capability/Regression/Test) 필수
- **Grader**: code-based — T19.1~T19.8 (8 assertion)

### C6. NORTH_STAR 본 repo 자기 적용 (v27.13.0 dogfood)
- **AC-6.1**: `docs/NORTH_STAR.md` 생성 (7섹션: Statement/NSM/Boundaries/Phase/Decision/Versioning/Changelog)
- **AC-6.2**: Statement가 사용자 정의에 부합 ("구체 디렉션 없이 고수준 서비스 한 번에")
- **AC-6.3**: NSM 명시 (HITO ≤ 3) + 2차 지표 4개
- **AC-6.4**: 4-gate 실제 적용 이력 (v27.8~v27.12 Pass/Fail 사례) 기록
- **Grader**: human review + grep-based — `test "$(wc -l < docs/NORTH_STAR.md)" -ge 100` 

### C7. 보안 sanitize (v27.12.1)
- **AC-7.1**: `docs/dev/*`에서 `uzysjung/std-dev-boilerplate`, `uzysjung/dyld-vantage` literal 0건
- **AC-7.2**: 전체 repo grep에서 `uzysjung/(std-dev-boilerplate|dyld-vantage)` 0건
- **Grader**: code-based — `grep -rE 'uzysjung/(std-dev-boilerplate|dyld-vantage)' . | wc -l` == 0

---

## Regression

기존 기능 보호. 변경 전후로 깨지면 안 되는 baseline.

### R1. test-harness T1~T14 유지
- **Baseline**: commit cd71d59 (v27.7.0) — 111 PASS, FAIL 0
- **현재**: 149 PASS, FAIL 0 (base 111 + 신규 38)
- **Tests**: T1(JSON) / T2(bash syntax) / T3(13 hook unit) / T4(cherry-pick) / T5(9 track) / T6(global immutable) / T7(global cmd) / T8(SPEC drift) / T9(req trace) / T10(doc accuracy) / T11(E2E smoke) / T12(install catalog) / T13(multi-track) / T14(update mode)

### R2. 9 Track 병렬 설치 100% PASS
- `tooling, csr-supabase, csr-fastapi, csr-fastify, ssr-htmx, ssr-nextjs, executive, full, data` 전부 clean install + agents/hooks/mcp.json/settings.json/CLAUDE.md/allowlist 생성 확인

### R3. 기존 6 skill 유지 (미변경)
- `continuous-learning-v2, strategic-compact, spec-scaling, deep-research, eval-harness, verification-loop, agent-introspection-debugging, e2e-testing, market-research` 모두 경로/내용 보존

### R4. 글로벌 `~/.claude/` 미변경 (D16 보호)
- T6 assertion: `~/.claude/CLAUDE.md` mtime unchanged
- Phase 1~5 전 과정에서 PROJECT_DIR 사전 차단 로직 유지

### R5. git 히스토리 무결성
- main 브랜치에 8 commit 추가 + 8 tag 생성, force push 0건
- v27.6.0(cd71d59)부터 linear history 유지

---

## Test (재현 절차)

세션 결과를 다시 검증하려는 누구든 아래 절차로 동일 결과 재현 가능.

### Setup
```bash
# 1. repo 클론 (or pull)
git clone https://github.com/uzysjung/uzys-claude-harness.git
cd uzys-claude-harness

# 2. 특정 태그 체크아웃
git checkout v27.13.0
```

### Run
```bash
# 전체 검증 (약 5-8분, 9 track 실설치 포함)
bash scripts/test-harness.sh

# 또는 빠른 검증 (T5/T11/T13/T14 skip, 약 5초)
bash scripts/test-harness.sh --quick
```

### Expected
```
========== Summary ==========
  Pass: 149
  Fail: 0
  Skip: 0
  Total: 149
```

### Specific sub-evals
| Capability | Command | Expected |
|-----------|---------|----------|
| C1 (v27.8.0) | `bash scripts/test-harness.sh 2>&1 \| grep -A 7 "T15\."` | 6 PASS |
| C2 (v27.9.0) | `bash scripts/test-harness.sh 2>&1 \| grep -A 8 "T16\."` | 6 PASS |
| C3 (v27.10.0) | `bash scripts/test-harness.sh 2>&1 \| grep -A 10 "T17\."` | 8 PASS (신규 T17.8 포함) |
| C4 (v27.11.0) | `bash scripts/test-harness.sh 2>&1 \| grep -A 9 "T18\."` | 7 PASS (신규 T18.7 포함) |
| C5 (v27.12.0) | `bash scripts/test-harness.sh 2>&1 \| grep -A 10 "T19\."` | 8 PASS |
| C6 (v27.13.0) | `test -f docs/NORTH_STAR.md && wc -l docs/NORTH_STAR.md` | file exists, ≥100 lines |
| C7 (v27.12.1) | `grep -rE 'uzysjung/(std-dev-boilerplate\|dyld-vantage)' . 2>/dev/null \| wc -l` | 0 |

### External E2E
```bash
# install.sh end-to-end (file:// repo)
TMP=$(mktemp -d) && cd "$TMP" && git init -q && \
  UZYS_HARNESS_REPO="file://$(git -C - rev-parse --git-dir 2>/dev/null || echo "/Users/uzysjung/Development/Claude-Workspace/uzysClaudeUniversalEnv/.git")" \
  bash /path/to/install.sh --track tooling --project-dir .
# Expected: exit 0, CLAUDE.md + .claude/agents/ + .mcp.json 생성
```

---

## Session Metrics (HITO baseline 수집용)

NSM 측정을 위한 raw 관측값. Phase 2 진입 시 이 데이터가 baseline이 됨.

| Metric | 값 | 참조 |
|--------|-----|------|
| 총 세션 시간 | ~7시간 | compaction 후 시작 → v27.13.0 push까지 |
| Features 완료 | 7 (v27.6~v27.13.0 — v27.8/v27.12.1 포함, NORTH_STAR는 사용자 input 있어 단독 feature) | |
| 사용자 HITO (명시적 개입) | **6회** | (1) 초기 SPEC — "railwayapp/railway-skills...", (2) 접근법 합의 — "OK 그럼 이제 최종...", (3) 진행 승인 — "OK 진행해", (4) 보안 스캔 옵션 선택 — "A 진행하자", (5) push 승인 — "push 해", (6) NORTH_STAR statement 제공 |
| HITO per feature | 6/7 ≈ **0.86** | 목표 ≤ 3. 현재 달성. 단 test 샘플이 1 세션뿐이라 baseline 부족 |
| Revision loop (자동 수정) | 4회 | (1) exec </dev/tty Device not configured, (2) marketplace add 공백 사라짐, (3) T15 grep \| escape, (4) T18 |/캡처 키워드 grep |
| Failed commits | 0 | 모든 commit 최초 시도 성공 |

> **주의**: HITO는 "명시적 지시 + 승인 + 의견"만 포함. 자동 task notification 등은 제외. 한 세션 표본이라 통계적 의미 제한적 — Phase 2에서 3+ 프로젝트 적용 후 재측정.

---

## Status

**READY FOR SHIP** — v27.13.0 main push + GitHub Release 7개 생성 완료. test-harness 149/149 PASS.

### 후속 작업
- Phase 2 진입: 실전 프로젝트 1-2개에 harness 적용해서 HITO baseline 3+ 데이터 확보
- C (P10 재평가): 분기 1회 scaffold 제거 검토
- 이 eval을 다음 세션 시작 시 참조 (연속성 유지)
